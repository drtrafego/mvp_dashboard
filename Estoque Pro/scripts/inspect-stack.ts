
import { stackServerApp } from "@/lib/stack"

console.log(Object.keys(stackServerApp));
// Try to inspect prototype or definitions if possible, or just assume standard behavior.
// Actually, I will check the node_modules types definition file.
